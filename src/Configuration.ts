// required variables
export const CANDIDATE_ID = process.env.CANDIDATE_ID || process.exit();
export const API_BASE_URL = process.env.API_BASE_URL || process.exit();
export const MAP_URL = process.env.MAP_URL || process.exit();
